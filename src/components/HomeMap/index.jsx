import React from 'react';
import {View, Text} from 'react-native';

const HomeMap = (props) => {
  return (
    <View
      style={{
        height: 300,
        backgroundColor: 'green',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>I am am map!</Text>
    </View>
  );
};

export default HomeMap;
